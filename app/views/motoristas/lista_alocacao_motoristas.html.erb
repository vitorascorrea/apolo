Associe um motorista para o velório da NCF <%= @chamado.nota %> <br>
<br><br><br>

<table border='1'>
	<tr>
		<th>RF do Motorista</th>
		<th>Nome</th>
		<th>Nº do Comunicador</th>
		<th>Alocar Motorista?</th>
	</tr>
	<% @motoristas.each do |m| %>
	<tr>
		<td><%= m.id %></td>
		<td><%= m.nome %></td>
		<td><%= m.n_comunicador %></td>
		<td><%= link_to "Alocar", associa_motorista_path(:id => m.id, :id_chamado => params[:id_chamado]) %></td>
	</tr>
	<% end %>	
</table>

<%= link_to "Logout", logout_path, method: 'delete' %>